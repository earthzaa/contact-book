import React from 'react';
import AppContainer from '../containers/AppContainer';

const PageNotFound = () => {
    return (
        <AppContainer>
            Page does not found !
        </AppContainer>
    );
};

export default PageNotFound;